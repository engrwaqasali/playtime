query Referrals($offset: Int) {
    referrals(offset: $offset) {
        count
        items {
            id
            username
            avatar
            isActive
            gamesCount
            income
            createdAt
        }
    }
}