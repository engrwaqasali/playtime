query Tournament($type: RefTournamentType!) {
    tournament(type: $type) {
        position
        amount
        finish
        users {
            avatar
            username
            amount
            prize
        }
    }
}